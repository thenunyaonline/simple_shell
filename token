#include "shell.h"

char **tokenizar(char *line)
{
	char token, 
	char **command = NULL;
	int cpt = 0;

	token = strtok(line, " \t\n");
	while (token)
	{
		eg_prints("%s\n", token);
		token = strtok(str, " \t\n");
	}
	eg_prints("str = %s\n", str);
	i
	return (0);
}
