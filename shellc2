#include "shell.h"

/**
 * main - main simple shell funcion
 * @agc: argument count
 * @agv: arguments
 * Return - 0
 */

int main(int agc, char **argv)
{
	char *line = NULL;
	char **command = NULL;
	int status;
	(void) agc;
	(void) argv;

	while (1)
	{
		line = legilimens_line(); 
		if (line == NULL)
		{
			if (isatty(STDIN_FILENO))
				write(STDOUT_FILENO, "\n", 1);
			return (status);
		}

		free(line);

		command = 
		status = 
	}
}
